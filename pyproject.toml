[tool.poetry]
name = "kr-database"
version = "0.1.0"
description = ""
authors = ["Anton_Glazunov"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
psycopg2 = "^2.9.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
